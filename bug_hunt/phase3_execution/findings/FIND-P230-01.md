# FIND-P230-01

| Field | Value |
|-------|-------|
| **ID** | FIND-P230-01 |
| **Severity** | P2 |
| **Status** | CONFIRMED |
| **Title** | Trade size median+p95 recomputed over full 10k deque on every add_trade(s) |
| **Theme** | P2-30 |
| **Zones** | Z04 |
| **Taxonomy** | performance |
| **Location** | flowmap/ui/heatmap_widget.py:574-585,423-427,571 |
| **Sibling** | R08-H19 |
| **Wave** | W1 |
| **Discovered by** | Wave1 RENDER/VP/UX static |

### Repro
1. Fill `_trades` to maxlen 10000.
2. Each `add_trades` batch (every GUI tick with trades) builds `sizes = [t[1] for t in self._trades]` then `np.median` + `np.percentile(..., 95)`.
3. Burst replay / liquid markets: O(n) sort-like work on GUI thread each tick, stacked with paint dual overlay (dots + bubbles).

### Expected
Throttled percentile refresh (e.g. 100–250 ms) or online/streaming quantiles; paint uses cache (already does).

### Actual
Ingest path pays full scan every batch; `add_trade` path pays per call if used singly.

### Fix hint
Recompute only every N trades or elapsed ms; optional numpy array ring buffer.
