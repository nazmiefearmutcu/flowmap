# FIND-P217-07 — Live omits book_ticker and liquidation channels

| Field | Value |
|-------|-------|
| **ID** | FIND-P217-07 |
| **Severity** | P1 |
| **Theme** | P2-17 |
| **Zones** | Z06 |
| **Taxonomy** | data_source |
| **Taxonomy secondary** | correctness |
| **Status** | confirmed |
| **Location** | `flowmap/data/crypcodile_live.py:148–155` |
| **Sibling** | R06 H6, H7, R20 P1-08 |
| **Wave** | W1 |
| **Discovered by** | Wave1-REPLAY/LIVE/CCXT |
| **Created** | 2026-07-13 |

### Repro
1. Compare live `make_connector(... channels=...)` vs replay `_dispatch_record` / replay worker channels.
2. Live: `["trade", "book_snapshot", "book_delta"]` only.
3. Replay worker supports `book_ticker` → BBO and `liquidation` → Trade(is_liquidation=True).
4. Live never produces native BBO records or liquidation flags from exchange channels.

### Expected
Parity with dispatch capability: include `book_ticker` and `liquidation` when exchange supports them (or document deliberate subset).

### Actual
```python
connector = make_connector(
    ...
    channels=["trade", "book_snapshot", "book_delta"],
    ...
)
```
BBO only if derived downstream from book; liquidations absent → magenta/cyan liq overlay never fed from live Crypcodile path. CCXT path *does* watch liquidations (asymmetry).

### Fix hint
Add channels when registry/exchange supports; map via existing `_dispatch_record` handlers.
