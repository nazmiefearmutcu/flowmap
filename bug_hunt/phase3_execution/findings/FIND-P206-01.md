# FIND-P206-01

| Field | Value |
|-------|-------|
| **ID** | FIND-P206-01 |
| **Severity** | P0 |
| **Status** | REFUTED |
| **Title** | get_volume_delta returns NaN before first trade |
| **Location** | flowmap/core/order_book.py:349-357 |
| **Theme** | P2-06 |
| **Taxonomy** | correctness |
| **Wave** | W1 |
| **Expected** | 0.0 (finite session delta before first trade) |
| **Actual (current tree)** | **0.0** — code returns `0.0` when `trade_count==0` (docstring explicitly forbids NaN for GUI/status). |
| **Prior claim** | Static Phase-3 pass claimed `math.nan`; that path is no longer present in source. |
| **Repro steps** | 1. `OrderBook('TEST').get_volume_delta()` → 0.0. 2. After trades buy2 sell3 → -1.0. 3. `reset()` then get_volume_delta → 0.0. 4. Status format `f"CVD: {cvd:+.0f}"` → `CVD: +0`. |
| **Evidence** | probe V1/V4/P2 all REFUTED; source lines 349-357. DensityEngine still appends raw `cvd` (:169) but MainWindow now supplies finite 0.0. |
| **Fix hint** | Already applied at API. Keep unit test locked to finite. Optional: coerce at DensityEngine boundary for defensive defense-in-depth if other callers pass nan. |
| **Sibling** | R17 H-N1, R20 P0-10; FIND-P206-02/04 also REFUTED |
| **Discovered by** | Wave1 BOOK hunter (deepen of static FIND-P206-01) |
| **Notes** | Deepen: production path main_window.py:958-959 / :985-996 no longer emits nan string under empty book. Residual risk only if a future caller passes float('nan') into push_snapshot. |
