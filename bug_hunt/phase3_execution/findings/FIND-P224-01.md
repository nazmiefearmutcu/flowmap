# FIND-P224-01 — CCXT order book identity stall (historical; nonce fix landed)

| Field | Value |
|-------|-------|
| **ID** | FIND-P224-01 |
| **Severity** | P0 |
| **Theme** | P2-24 |
| **Zones** | Z17 |
| **Taxonomy** | correctness |
| **Status** | fixed |
| **Location** | `flowmap/data/crypto.py:209–260`, `290–318` |
| **Sibling** | R06 H1, R20 P0-12 |
| **Wave** | W1 |
| **Discovered by** | static → Wave1 re-verify |
| **Created** | 2026-07-13 |
| **Notes** | Original bug: `if ob is not None and ob is not last_ob` with ccxt.pro in-place mutation. **W1 REPLAY/LIVE/CCXT re-verify:** sender now uses `_orderbook_nonce` / `_ticker_nonce` bumped in watch loops; emit when `ob_nonce != last_ob_nonce`. Residual: dual BBO (FIND-P224-04); dead `last_ob` locals; WS path still omits receive_timestamp. |

### Original expected / actual
- Expected: emit on each book content update  
- Actual (pre-fix): identity gate skipped all updates after first  

### Fix present
```python
self._orderbook_buffer = ob
self._orderbook_nonce += 1
# sender:
if ob is not None and ob_nonce != last_ob_nonce:
    ... emit snapshot + bbo ...
    self._last_ob_nonce = ob_nonce
```

### Regression test hint
Unit: same dict object mutated; nonce increments; assert N emits == N nonce bumps.
