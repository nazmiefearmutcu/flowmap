# FIND-P226-02

| Field | Value |
|-------|-------|
| **ID** | FIND-P226-02 |
| **Severity** | P2 |
| **Status** | CONFIRMED |
| **Title** | rebuild_heatmap success path omits view_changed — VP/pulse sync lag after zoom |
| **Theme** | P2-26 |
| **Zones** | Z01, Z14 |
| **Taxonomy** | rendering |
| **Location** | flowmap/ui/heatmap_widget.py:735-742 vs 872-876 |
| **Sibling** | R08-H14 |
| **Wave** | W1 |
| **Discovered by** | Wave1 RENDER/VP/UX static |

### Repro
1. Early-exit branch of `rebuild_heatmap` (~735-742) emits `view_changed`.
2. Successful full rebuild ends at `_last_*` update, `_sync_vwap`, `_cache_dirty`, `update()` — **no** `view_changed.emit()`.
3. MainWindow `_on_heatmap_view_changed` refreshes volume_profile levels/row_height; without emit, VP can keep stale price list until next live push (auto_follow) emits view_changed.

### Expected
Any geometry/center change that completes rebuild notifies dependents.

### Actual
Asymmetric emit; zoom/Go Live rebuild can leave VP rows desynced (compounds FIND-P234-01).

### Fix hint
Emit `view_changed` once at end of successful rebuild (and keep early-exit emit).
