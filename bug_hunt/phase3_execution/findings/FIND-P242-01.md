## FIND-P242-01 — Dual converters: embed dict path ≠ standalone `_dispatch_record`

| Field | Value |
|-------|-------|
| Severity | P1 |
| Theme | P2-42 |
| Zones | Z12 |
| Taxonomy | integration |
| Status | confirmed |
| Location | Embed: `flowmap_window.dict_to_flowmap_objects` L14–78; Standalone: `flowmap/data/crypcodile_replay.py:_dispatch_record` L174–208 |
| Sibling | R02 dual conversion, P2-36/38 channel gaps |
| Wave | W1 |
| Created | 2026-07-13 |
| Discovered_by | Wave1-EMBED |

### Repro
1. Compare channel coverage:

| Channel | `_dispatch_record` | `dict_to_flowmap_objects` |
|---------|--------------------|---------------------------|
| trade | yes (+ liquidation attr) | yes (`liquidation` key) |
| book_snapshot | yes | yes |
| book_delta / is_snapshot | yes | yes |
| book_ticker → BBO | **yes** | **no (returns [])** |
| liquidation channel | **yes** | **no** |

2. Hist scans only snapshot/delta/trade — BBO/liquidation never enter embed path even if present in lake.
3. Standalone live/replay use one converter; embed hist uses a second, independently maintained copy.
4. No differential unit tests between the two.

### Expected
Single shared converter module owned by FlowMap; embed imports only. Golden-row equality tests as merge gate.

### Actual
Silent drift risk: field/side/liq/BBO differences between CLI-hist and REPLAY/LIVE for same lake rows. Liquidation bubbles and ticker BBO absent from hist only.

### Fix hint
Delete `dict_to_flowmap_objects` or thin-wrap shared `_dispatch_record` + dict adapter. Contract tests on golden parquet rows.

### Evidence
- Embed returns `[]` for unknown channels (L78)
- Replay handles `book_ticker` / `liquidation` tags (L202–205)
