## FIND-P238-03 — Empty catalog hist returns with no user feedback

| Field | Value |
|-------|-------|
| Severity | P1 |
| Theme | P2-38 |
| Zones | Z12 |
| Taxonomy | data_source |
| Status | confirmed |
| Location | `Crypcodile/src/crypcodile/gui/flowmap_window.py:147-148` |
| Sibling | R05 H3 empty loop (replay), UX empty lake |
| Wave | W1 |
| Created | 2026-07-13 |
| Discovered_by | Wave1-EMBED |

### Repro
1. Point `data_dir` at empty / wrong lake or unknown symbol.
2. All three scans empty → `events == []`.
3. `if not events: return` — before any reset/message/status.
4. Outer `__init__` only logs load **exceptions** to stderr; empty success is silent.
5. CLI still opens window; 500ms later MainWindow auto-starts live feed.

### Expected
User-visible “No historical data for {symbol} in range” in status bar / dialog; optional skip of confusing blank hist.

### Actual
Bare return; blank hist; live may start with empty book. Operators assume data missing vs code path bug.

### Fix hint
Status message + log missing channel paths; unify empty UX with replay. Do not treat empty as success.

### Evidence
```python
if not events:
    return
```
