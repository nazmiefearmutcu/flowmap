# FIND-P239-03 — Full-history trade materialize (OOM risk)

| Field | Value |
|-------|-------|
| **ID** | FIND-P239-03 |
| **Severity** | P0 |
| **Theme** | P2-39 |
| **Zones** | Z07, Z09 |
| **Taxonomy** | memory |
| **Taxonomy secondary** | performance |
| **Status** | confirmed |
| **Location** | `flowmap/data/crypcodile_replay.py:335–357` |
| **Sibling** | R05 H6, FIND-P219-06 |
| **Wave** | W1 |
| **Discovered by** | Wave1-REPLAY/LIVE/CCXT |
| **Created** | 2026-07-13 |

### Repro
1. Large lake: days/weeks of trades for one symbol.
2. Select short book window; start replay.
3. Watch process RSS during `list(book_iter)` + `list(trade_iter)` over global trade min→max.
4. Auto-loop refetches both lists each cycle.

### Expected
Stream/merge iterators by timestamp within selected window; bounded buffers.

### Actual
`records = list(book_iter)` and `raw_trades = list(trade_iter)` with `limit=None` materialize full series in RAM for sort, EMA, warp, and alignment. Auto-loop multiplies peak IO/RAM.

### Fix hint
Windowed trade fetch; heap-merge without full list; abort checks during materialize (pairs FIND-P219-02).
