## FIND-P209-01 — One-shot tick detect; refine branch dead; `detect_tick_size` ignored

| Field | Value |
|-------|-------|
| **ID** | FIND-P209-01 |
| **Severity** | P0 |
| **Theme** | P2-09 |
| **Zones** | Z03 |
| **Taxonomy** | correctness |
| **Status** | FIXED |
| **Location** | `flowmap/engine/density_engine.py:114,119-131` |
| **Sibling** | R17 H-T1–T3, FIND-NUM-06, R20 P0-08 |
| **Wave** | W1 |
| **Created** | 2026-07-13 |
| **Discovered_by** | P3-DENSITY Wave1 |
| **Notes** | Deepens FIND-NUM-06 with param + dead-else facts |

### Repro (unit probes)

```python
from flowmap.engine import DensityEngine
from flowmap.core import BookLevel, BBO

def levels_at(*prices, size=1.0):
    return [BookLevel(price=p, bid_size=size) for p in prices]

e = DensityEngine()
e.resize(20, 8)
bbo = BBO(0, "T", 100.0, 100.1, 1, 1)

# L2: sparse first book (true tick 0.1, only 1.0 gaps visible)
e.push_snapshot(levels_at(100.0, 101.0), bbo)
assert e.tick_size == 1.0 and e._tick_size_detected  # locks 1.0

# L3: denser second snap cannot refine (else branch unreachable)
e.push_snapshot(levels_at(100.0, 100.1, 100.2, 101.0), bbo)
assert e.tick_size == 1.0  # STUCK — never min-updates

# L5: detect_tick_size=False still locks
e2 = DensityEngine(); e2.resize(20, 8)
e2.push_snapshot(levels_at(50.0, 50.5), bbo, detect_tick_size=False)
assert e2._tick_size_detected and e2.tick_size == 0.5  # param ignored
```

### Code structure
```119:131:flowmap/engine/density_engine.py
        if not getattr(self, '_tick_size_detected', False):
            prices = sorted([lv.price for lv in levels])
            if len(prices) >= 2:
                diffs = np.diff(prices)
                valid_diffs = diffs[diffs > 0.000001]
                if len(valid_diffs) > 0:
                    obs_min = round(float(np.min(valid_diffs)), 6)
                    if not getattr(self, '_tick_size_detected', False):
                        self.tick_size = obs_min
                        self._tick_size_detected = True
                    else:
                        self.tick_size = min(self.tick_size, obs_min)  # DEAD
```
Outer `if not detected` + inner set True → `else: min(...)` never runs. Comment "keep running minimum" is false.

`detect_tick_size: bool = True` in signature is never read in body (callers like rebuild single-column pass `False` ineffectively).

Rebuild one-shot: `heatmap_widget.py:619-632` takes **first** multi-level history entry min gap + `break` — same poison if open is sparse.

### Expected
Exchange tick meta, multi-sample min with threshold, or continuous refine until stable; honor `detect_tick_size`.

### Actual
Permanent wrong vertical grid after sparse first book; param dead; default 0.05 until first multi-level.

### Fix hint
Prefer instrument tickSize; else running min over N snaps before freeze; remove dead else or invert logic to always `min` while sampling; gate on `detect_tick_size`.

### Evidence
- unit L2/L3/L5 above
- sibling FIND-NUM-06

### Fix (2026-07-13)
Phase-4: `density_engine.py` — running `min()` over first 20 samples; honor `detect_tick_size=False`. See `bug_hunt/phase4_fixes/FIX_TICK_QUEUE.md`.
