# FIND-P202-02

| Field | Value |
|-------|-------|
| **ID** | FIND-P202-02 |
| **Severity** | P1 |
| **Status** | FIXED |
| **Title** | apply_bbo inserts zero-size bid/ask levels into book |
| **Location** | flowmap/core/order_book.py:136-151 |
| **Theme** | P2-02 |
| **Taxonomy** | correctness |
| **Wave** | W1 |
| **Expected** | size ≤ 0 → pop level (same as apply_update); never store zero-size keys; best sizes consistent |
| **Actual** | `if bbo.bid > 0: self._bids[bbo.bid] = bbo.bid_size` even when `bid_size==0`. Same for ask. Ghost zero-size TOB pollutes get_levels/DOM. |
| **Repro steps** | 1. `ob = OrderBook('T')`. 2. `ob.apply_bbo(BBO(1,'T',100,101,0.0,1.0))`. 3. Assert `100 in ob._bids and ob._bids[100]==0.0`. 4. Repeat with ask_size=0. |
| **Evidence** | probe C5/C5b: bids={100.0:0.0}; asks={101.0:0.0}. Contrast apply_update size<=0 pop path at :89-91. |
| **Fix hint** | If size<=0: pop price; only set best_* when size>0; then call `_recalc_bbo()` or shared uncross. |
| **Sibling** | H-R03-03 |
| **Discovered by** | Wave1 BOOK hunter |
