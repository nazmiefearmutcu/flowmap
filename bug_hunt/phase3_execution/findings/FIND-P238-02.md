## FIND-P238-02 — Hist preload has no snapshot/bootstrap gate

| Field | Value |
|-------|-------|
| Severity | P1 |
| Theme | P2-38 |
| Zones | Z12 |
| Taxonomy | data_source |
| Status | confirmed |
| Location | `Crypcodile/src/crypcodile/gui/flowmap_window.py:119-145, 202-213` |
| Sibling | R05 H5, P2-04 is_snapshot |
| Wave | W1 |
| Created | 2026-07-13 |
| Discovered_by | Wave1-EMBED |

### Repro
1. Channels scanned: `book_snapshot`, `book_delta`, `trade` only (no liquidation / book_ticker).
2. Scan failures become empty DataFrames (`except: snap_df = pl.DataFrame()`).
3. `dict_to_flowmap_objects`: pure `book_delta` without `is_snapshot` → `Level2Update`s applied on empty book.
4. Lake with only incremental deltas / trades only → sparse or empty density, no error.

### Expected
Require snapshot **or** first delta `is_snapshot=True`; else error panel with channel checklist. Missing channels logged.

### Actual
Partial lakes produce silent wrong/empty books. Real lakes often lack `book_snapshot` partitions (R05); bootstrap depends on delta `is_snapshot` without validation. Trade-only hist paints trades on empty density.

### Fix hint
Channel policy + preflight inventory; refuse start or show diagnostics when bootstrap impossible. Align with replay H5 handling.

### Evidence
- try/except empty DF per channel: lines 119–132
- apply path has no “book non-empty after first N events” assert
