## FIND-P209-02 — Symbol substring heuristics for ticks_per_row / bid_ref

| Field | Value |
|-------|-------|
| **ID** | FIND-P209-02 |
| **Severity** | P1 |
| **Theme** | P2-09 |
| **Zones** | Z03 |
| **Taxonomy** | data_source |
| **Status** | confirmed |
| **Location** | `flowmap/ui/source_manager.py:386-403` |
| **Sibling** | R17 T5, FIND-NUM-03 |
| **Wave** | W1 |
| **Created** | 2026-07-13 |
| **Discovered_by** | P3-DENSITY Wave1 |
| **duplicate_of** | FIND-NUM-03 (same root; theme-owned alias for P2-09) |

### Repro matrix
| Symbol string | Branch | ticks_per_row | bid/ask ref |
|---------------|--------|---------------|-------------|
| `binance-spot:SOLUSDT` | SOL | 2 | 3000 |
| `binance-spot:ETHUSDT` | ETH | 10 | 100 |
| `binance-spot:BTCUSDT` | else | 100 | 5 |
| `btcusdt` / `BTC-USDT` | else | 100 | 5 |
| `MYETHUSDT` | **ETH** (substring) | 10 | 100 |
| `SOL/USDT` | else (no SOLUSDT) | **100** | **5** |

```386:397:flowmap/ui/source_manager.py
            if "SOLUSDT" in self._symbol:
                engine.ticks_per_row = 2
                ...
            elif "ETHUSDT" in self._symbol:
                engine.ticks_per_row = 10
                ...
            else: # BTCUSDT
                engine.ticks_per_row = 100
                engine.config.bid_ref = 5.0
```

### Expected
Config/exchange meta for tick aggregation and refs; explicit asset table not substring traps.

### Actual
Fragile `"SOLUSDT" in symbol` / `"ETHUSDT" in symbol`; everything else treated as BTC-like (tpr=100, ref=5). Compounds FIND-P207-02 (max collapse) and FIND-P210-01 (polyline scale) on wrong symbols.

### Fix hint
Parse base asset after last `:` / before `USDT`; map via instrument table; allow user override.

### Evidence
- source_manager 386-403
- default engine path without source_manager keeps tpr=1, bid_ref=3000 (ctor) vs EngineConfig 20000 — see FIND-P211-06
