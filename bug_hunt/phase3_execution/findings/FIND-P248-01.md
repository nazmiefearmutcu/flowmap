## FIND-P248-01 — `console=False` + no crash log → silent death on start

| Field | Value |
|-------|-------|
| Severity | P0 |
| Theme | P2-48 |
| Zones | Z20 |
| Taxonomy | packaging |
| Status | FIXED |
| Location | `FlowMap.spec:29`; `flowmap/main.py` (no excepthook/log) |
| Sibling | R13-P3, R20 P0-13 |
| Wave | W1 |
| Created | 2026-07-13 |
| Discovered_by | Wave1-PACKAGING |

### Repro
1. `FlowMap.spec` EXE: `console=False`, `debug=False`.
2. Grep app code for `sys.excepthook`, log file under Application Support, or `sys.frozen` handlers — **none** for crash reporting.
3. Any import/runtime failure in frozen onedir/app shows Dock bounce / instant quit with no Terminal and no log path.
4. Compounded by FIND-P247 hardcoded `/Users/nazmi/data` and optional crypcodile/ccxt/OpenGL import paths.

### Expected
Debug build: `console=True`. Release: windowed + always-on log (`~/Library/Logs/FlowMap/flowmap.log`) + `sys.excepthook` writing traceback. Optional crash dialog.

### Actual
Windowed-only packaging with zero diagnostics surface. Ship failures are undebuggable for end users.

### Fix hint
Split `FlowMap.debug.spec` vs release; install excepthook early in `main.py`/`run_flowmap.py`; document log location in README.

### Evidence
```python
# FlowMap.spec
console=False,
```
No `sys.frozen` / `_MEIPASS` / excepthook in product code.
