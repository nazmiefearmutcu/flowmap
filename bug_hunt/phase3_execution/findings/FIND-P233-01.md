# FIND-P233-01

| Field | Value |
|-------|-------|
| **ID** | FIND-P233-01 |
| **Severity** | P1 |
| **Status** | FIXED |
| **Title** | DomLadder windows highest-N prices only — not BBO-centered; _depth unused |
| **Theme** | P2-33 |
| **Zones** | Z14 |
| **Taxonomy** | correctness |
| **Location** | flowmap/ui/dom/dom_ladder.py:132-135,192-196 |
| **Sibling** | R12-H03/H07, P2-33 G1 |
| **Wave** | W1 |
| **Discovered by** | Wave1 RENDER/VP/UX static |

### Repro
1. Feed deep book (hundreds of levels sorted ascending by price).
2. Open DOM dock with finite height → `visible_count = h // row_height + 2`.
3. `display_levels = self._levels[-visible_count:]` then reverse → only **highest** prices.
4. Call `set_depth(10)` — stored but paint ignores `_depth`.

### Expected
Window centered on BBO mid (or bid/ask) with `_depth` levels each side, matching trader ladder conventions and heatmap center.

### Actual
Deep crypto books show far ask stack; bids near mid often off-screen. Settings depth is a no-op for windowing.

```192:196:flowmap/ui/dom/dom_ladder.py
        visible_count = h // self.row_height + 2
        display_levels = self._levels[-visible_count:]
        display_levels.reverse()
```

### Fix hint
Find index of best bid/ask (or mid); slice `[mid_idx - n//2 : mid_idx + n//2]`; use `_depth` or fit-to-height count. Optional scroll with wheel (see FIND-P233-02).
