# FIND-P202-01

| Field | Value |
|-------|-------|
| **ID** | FIND-P202-01 |
| **Severity** | P0 |
| **Status** | REFUTED |
| **Title** | Crossed book dual-prune wipes both sides |
| **Location** | flowmap/core/order_book.py:381-424 |
| **Theme** | P2-02 |
| **Taxonomy** | correctness |
| **Wave** | W1 |
| **Expected** | Dual simultaneous prune using pre-BBO empties book when every bid ≥ best_ask and every ask ≤ best_bid |
| **Actual** | Current code uses **iterative uncross**: drop crossed bids first, recompute BBO, then drop remaining crossed asks. Simple TOB cross `bid=100 ask=99` → `bids={}` `asks={99:1}` — **not both empty**. Locked `bid==ask=100` same pattern. |
| **Repro steps** | 1. `cd /Users/nazmi/flowmap && .venv/bin/python bug_hunt/phase3_execution/W1/probe_order_book_wave1_stdlib.py` (or import OrderBook). 2. `ob.apply_snapshot(Level2Snapshot(..., bids=((100,1),), asks=((99,1),)))`. 3. Assert `len(ob._bids)==0 and len(ob._asks)==0` fails. |
| **Evidence** | W1/probe_order_book_wave1_results.json C1/C3; source comment lines 381-386 "Do NOT prune both sides using the same pre-prune BBO". Sibling residual: FIND-P202-06 (mid=0 after bid wipe). |
| **Fix hint** | N/A for dual-wipe (already mitigated). Prefer size-aware side choice on pure TOB cross to keep mid finite — see FIND-P202-06. |
| **Sibling** | H-R03-01 |
| **Discovered by** | Wave1 BOOK hunter (stdlib mirror + source trace) |
